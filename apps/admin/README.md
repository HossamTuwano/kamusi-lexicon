# apps/admin (planned)

Expert moderation and curation for the Swahili Kamusi.

Uses moderator/admin roles from the API (`POST /api/entries/:id/moderate`).
Must consume `@kamusi/core` types — never redefine Lemma/Sense locally.
