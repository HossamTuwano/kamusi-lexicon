{
  "name": "kamusi-lexicon",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "api": "npm run start --workspace=api",
    "install:all": "npm install"
  }
}
