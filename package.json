{
  "name": "kamusi-lexicon",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build:packages && npm run build --workspace=api",
    "build:core": "npm run build --workspace=@kamusi/core",
    "build:database": "npm run build --workspace=@kamusi/database",
    "build:packages": "npm run build:core && npm run build:database",
    "build:api": "npm run build:packages && npm run build --workspace=api",
    "build:web": "npm run build:packages && npm run build --workspace=web",
    "build:admin": "npm run build:packages && npm run build --workspace=admin",
    "build:all": "npm run build:packages && npm run build --workspace=api && npm run build --workspace=web && npm run build --workspace=admin",
    "start": "npm run api:prod",
    "api": "npm run start --workspace=api",
    "api:prod": "npm run start:prod --workspace=api",
    "api:dev": "npm run start:dev --workspace=api",
    "web:dev": "npm run dev --workspace=web",
    "test": "npm run test --workspace=api",
    "test:e2e": "npm run test:e2e --workspace=api",
    "install:all": "npm install"
  },
  "dependencies": {
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.1"
  },
  "devDependencies": {
    "@nestjs/common": "^11.0.0",
    "@nestjs/core": "^11.0.0",
    "@nestjs/platform-express": "^11.0.0",
    "@types/bcrypt": "^6.0.0",
    "@types/passport-jwt": "^4.0.1",
    "reflect-metadata": "^0.2.0",
    "rxjs": "^7.8.0"
  }
}
